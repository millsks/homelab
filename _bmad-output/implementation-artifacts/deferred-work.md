# Deferred Work

Findings that are real but not the responsibility of the story that surfaced them.
Append-only. Do not edit existing entries.

**One permitted amendment: a `resolution:` line.** A later story that closes an entry appends that
line to it, naming itself and what it did. Nothing already written is reworded or removed — the
finding and its evidence stay exactly as the story that surfaced them left them. Without this, a
ledger only grows and stops being readable as a work list, which is the failure this file exists to
avoid; with it, closure is visible beside the finding rather than inferred from silence. Story 1.1
already did this informally by editing a summary in place; making it a named field is the same
thing done in a way that preserves the original text.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-repository-skeleton-with-layered-ownership.md`
  summary: `pixi run bootstrap` is broken — the task runs `pre-commit install --install-hooks` but no `.pre-commit-config.yaml` exists.
  evidence: Pre-existing from the pixi workspace commit, not caused by story 1.1. Anyone following the README onboarding hits it. Natural home is story 1.3, which owns the convergence harness — pre-commit is where a commit-time check for AD-15's plaintext-secret rule would live.
  resolution: CLOSED by story 1.4. `.pre-commit-config.yaml` now exists and `pixi run bootstrap` succeeds, installing both the `pre-commit` and `commit-msg` hook types from the config's own `default_install_hook_types`. Every hook is `repo: local` with `language: system`, shelling out through `pixi run`: the second-toolchain objection story 1.3 raised is answered by not having a second toolchain at all, so `--install-hooks` needs no network and a commit runs exactly the tools `pixi.lock` resolved. The conventional-commit check is therefore ours — `src/asgard_harness/commit_message.py`, thirty lines, with tests — rather than a remote hook repository pinned by a `rev` this Repository does not control.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-repository-skeleton-with-layered-ownership.md`
  summary: No `.github/workflows/` — the CI gate runs only on a developer machine, so "the gate is the definition of done" holds locally and nowhere else.
  evidence: `.github/` contains only vendored agent definitions. The project scaffold standard expects a CI workflow and a status badge. Pre-existing.
  resolution: CLOSED by story 1.3. `.github/workflows/ci.yml` runs `pixi run ci` on every push and pull request to `main`, across a matrix of every interpreter in the supported range, plus two scheduled jobs (`drift`, `converge`) whose records are uploaded as artifacts. `fetch-depth: 0` is deliberate — the stale-provenance detector asks git which commit last touched `epics.md`, and a shallow clone cannot answer, so the check would degrade to SKIP, which does not fail the gate. Actions are pinned to commit SHAs rather than floating `@v4` tags, and `pixi-version` is pinned exactly, because AD-20 prohibits `latest` and a moving tag is `latest` wearing a version number. The status badge is still absent; that is cosmetic and stays open below.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-repository-skeleton-with-layered-ownership.md`
  summary: Dev dependencies are pinned `*` in `pixi.toml` while the same repository declares `latest` prohibited under AD-20.
  evidence: `ruff`, `mypy`, `pre-commit`, `yamllint`, `ansible-lint` are all unpinned. Sharpened by story 1.1: the `.yamllint` rules added there are shaped to satisfy whatever ansible-lint currently wants, so an unpinned ansible-lint can silently break the gate. Pre-existing from the pixi workspace commit.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-repository-skeleton-with-layered-ownership.md`
  summary: No LICENSE file in a public repository, so the default is all-rights-reserved.
  evidence: Repository visibility is public. README's License section reads "Not yet declared". A deliberate choice is fine; an accidental one is not.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-repository-skeleton-with-layered-ownership.md`
  summary: `tofu-validate` scans exactly one level (`tofu/*/`), so a root module nested deeper is skipped silently.
  evidence: Same class of defect as the one story 1.1 fixed, one level down — a validator that reports success over configuration it never read. Harmless while the tree holds no `.tf` files; must be revisited the moment real OpenTofu configuration lands, which is story 2.6.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-repository-skeleton-with-layered-ownership.md`
  summary: RESOLVED during story 1.1 — `_bmad-output/` is now excluded from yamllint rather than having its document-start marker maintained by hand.
  evidence: The predicted failure occurred within minutes of being predicted: the sprint-status sync rewrote the file without `explicit_start` and `yamllint --strict` failed on it. Kept here because the lesson generalises — generated artifacts should never be held to a house style that the generating tool does not know about, or two tools end up fighting over one file.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-repository-skeleton-with-layered-ownership.md`
  summary: Nothing enforces the upward-dependency prohibition that the design calls its single enforceable claim.
  evidence: No check inspects a layer directory for references to a higher layer. The README now states the claim is enforceable in principle rather than enforced. Natural home is story 1.3's harness.
  resolution: CLOSED by story 1.3. `asgard_harness.checks_crossdoc.check_layer_dependencies` walks every `<tool root>/l<N>-*/` directory and fails on any file referencing an `l<M>-` layer where `M > N`, naming the file and the layers. It counts only files it actually decoded, and names any it could not, because in this harness the count is the claim. The detector is textual: it matches layer tokens, not resolved dependencies. That cuts both ways, and both were observed during this story — it caught a genuine upward reference in a fixture, and it also fired on this story's own Runbook when a sample output block quoted the push-based layer names in prose. The Runbook was reworded rather than the detector relaxed, but a Runbook that legitimately needs to name a higher layer in prose will hit this, and the answer will have to be a scoping rule rather than an exemption list.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-repository-skeleton-with-layered-ownership.md`
  summary: No Markdown or intra-repository link checking anywhere in the gate.
  evidence: Story 1.1 added roughly 500 lines of Markdown whose entire value is cross-references, and the gate lints Python, YAML, Ansible and OpenTofu — none of which touch it. A link checker would have caught the three dangling references the review found by hand.
  resolution: PARTIALLY CLOSED by story 1.3. The audit now resolves the cross-references that carry meaning: every Index Runbook and Automation path against the filesystem, every ownership `Procedure` cell against the Index's key set, every Runbook's front matter against its Index entry and back, and every Runbook's headings against the template. What is still unchecked is ordinary Markdown link syntax — a `[text](path)` to a file that does not exist, anywhere outside those columns. Left open deliberately rather than folded in: a general link checker is a different tool with a different failure mode (it wants to be right about anchors and external URLs), and bolting it onto the harness would blur what the harness is for.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-repository-skeleton-with-layered-ownership.md`
  summary: No per-(tool root x layer) mapping records which layer directories are expected-empty by design.
  evidence: 24 directories were created; roughly half will never hold anything — Kubernetes manifests for physical racking do not exist. Nothing distinguishes expected-empty from an unowned defect, so the audit cannot tell them apart. README was softened to stop claiming OWNERSHIP.md answers this.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: The spec's Intent says `PROCEDURE-INDEX.md` defines nine defect classes; it defines sixteen.
  evidence: Counted from § "Defects this Index reports": six entry-level, two namespace-level, four Index-to-story, four cross-document. `docs/OWNERSHIP.md` § Audit defines four more, one of which is the Index's incomplete-Procedure rule named for completeness, so five ownership-side rules in total including verification-present. The harness implements every one of them except the unowned defect, which is separately recorded below. The figure in the spec is stale rather than wrong-headed — it predates the Index's final defect list — but a spec that undercounts what it requires by seven is a spec someone will later read as complete when it is not. Flagged rather than silently corrected: the Intent block is frozen and human-owned.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: The unowned defect in `docs/OWNERSHIP.md` § Audit cannot be implemented as defined, and is reported by the harness rather than approximated.
  evidence: "A configurable resource class present in the platform and absent from this table" needs an enumeration of the platform's resource classes that is independent of the table — and the table is the only such enumeration. Nothing the harness can read distinguishes an unowned class from a class nobody has thought of. `pixi run audit` therefore reports it as `[SKIP]` with that reason rather than as a pass; a check that cannot see the defect it is named for must not report success. Closing it needs a second enumeration — plausibly a discovered inventory from running systems, which is a much later epic — or a human decision to rescope the rule. Surfaced under the spec's "Ask First" clause; no definition was narrowed.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: The unwritten-human-form detector was implemented as an agreement check against the Index's own record, not as a bare "the file is absent" failure. This is an interpretation and needs human confirmation.
  evidence: The Index defines the defect as "an entry whose Runbook path does not exist" and then says most are unwritten today and that is expected while their owning stories are unstarted. Taken literally the audit must fail on seven of the nine `manual-by-decision` entries today, which contradicts the spec's own acceptance criterion that a clean repository exits 0. The Index resolves this itself: it says Runbook presence for these entries "is tracked in Deliberately manual work", which has a `Human form written?` column, and Totals carries a `Human forms written` figure. The detector therefore fails when that column disagrees with disk in either direction, and Totals recomputes the count from the filesystem — so the number cannot quietly stop shrinking, which is the reason the Index gives for the rule existing. If the human intent was the literal reading, the fix is a change to the Index's wording, not to the detector.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: `pixi run bootstrap` is still broken — no `.pre-commit-config.yaml` exists. The story that was expected to close it did not.
  evidence: Deliberate. The entry above nominates story 1.3 because "pre-commit is where a commit-time check for AD-15's plaintext-secret rule would live" — but that check is story 1.4's, and shipping a hook configuration whose only real hook belongs to the next story means shipping one that has never enforced anything. A pre-commit config also pulls its own tool environments over the network at install time, independent of `pixi.lock`, which is a second unpinned toolchain in a repository that prohibits `latest`. Both are 1.4's problems to weigh when it adds the plaintext-secret hook it actually needs.
  resolution: CLOSED by story 1.4, and the deferral was right: the hook now ships with the check that gives it a reason to exist, and it has been proven to fire before being declared done. The second-toolchain problem was weighed rather than accepted — all-local hooks, no remote `rev`, no network at install time.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: Compiled Python bytecode is committed under `.agents/` and `.claude/`.
  evidence: Ten `__pycache__/*.pyc` files from vendored BMAD skill scripts, built by CPython 3.11 — not even this workspace's interpreter. Story 1.3 added `__pycache__/` and `*.py[cod]` to `.gitignore`, which stops any new ones, but `.gitignore` does not untrack what is already tracked. Untracking them is `git rm --cached` over paths the story's spec marks read-only, so it was not done here. It is a one-line change whenever someone is deliberately touching the vendored trees.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: `src/asgard_harness/__main__.py` is the one file the test suite does not execute.
  evidence: Coverage is 98% overall with that module at 0%: its body only runs under `python -m asgard_harness`, which the tests reach through `cli.main` instead. The module is five lines with no branch of its own, so the risk is a typo in the module-level wiring rather than logic — and `pixi run audit`, `selfcheck`, and `drift` all invoke it for real in the gate, so a broken `__main__` fails CI immediately. Recorded because "98%" should not read as "everything ran".

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: RESOLVED during story 1.3 review — the convergence checks read a failed tool run as a clean one, the fourth instance of this project's recurring defect.
  evidence: `check_idempotence` and `check_drift` branched only on `run.changed`. Every parser set an exit code and no decision consulted it, and the ansible runner discarded it outright, so `tofu plan` in an uninitialised module, `kubectl diff` with no reachable cluster, and a missing binary all produced `changed=()` and reported PASS. Invisible only because no Automation exists yet; it would have gone live with the first OpenTofu module. Fixed by encoding each tool's own exit-code convention in its own parser (tofu 2=changes/1=error, kubectl 1=differences/>1=error, ansible any non-zero=error), adding `RunResult.error`, and refusing to judge a run that carries it. Seven runner fixtures and 24 tests now fail if the behaviour is reverted, verified by mutation.
  pattern: This is the same error as story 1.1's `&& cmd || echo` gate guard, the review's own `pixi run ci && echo PASS || echo FAIL`, and the self-check's untested failure branch — treating "no positive evidence of a problem" as "evidence of no problem". A tool that could not run, a branch never taken, and a genuinely clean result all produce the same shape, and nothing distinguished them. The general defence adopted here: the test to write is the one that breaks the thing and asserts the break is reported, never the one that runs it clean and asserts success.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: A SKIPPED check does not fail the gate, so any detector degrading to SKIP would leave the gate green with one rule silently unenforced.
  evidence: `AuditReport.exit_code` counts only FAILED, by design — an honest skip is not a defect. But that makes SKIP a silent-degradation channel: a shallow clone silences the provenance check, a missing template silences the Runbook-shape check, and the gate stays green. Mitigated rather than closed: `audit.EXPECTED_AUDIT_SKIPS` names the one legitimate skip, and an integration test asserts the skipped set equals exactly that, so a new skip fails CI. Not closed because the mitigation is a pinned list a human maintains; a structural fix would need skips to carry an expiry or an owner.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: The merge gate would have reached live infrastructure the moment the first role landed.
  evidence: The convergence suite sat inside `run_audit`, which is in `pixi run ci`. Every pull request would have shelled out to `ansible-playbook`, `tofu plan`, or `kubectl diff` against managed hosts — contradicting this Procedure's own Runbook, whose preconditions said "no credentials of any kind" and whose rollback said the harness "does not write to any managed system". RESOLVED: `run_audit` is now a pure reader and the convergence suite moved to `run_convergence`, scheduled alongside `drift`. `cli.GATE_COMMANDS` pins what `all` runs, and an integration test monkeypatches `run_command` to raise if the audit shells out at all. The Runbook's precondition table was corrected rather than left to be believed.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: GitHub Actions are pinned to commit SHAs, which nothing renews.
  evidence: AD-20 prohibits `latest`, and `actions/checkout@v7` is a moving tag, so the actions are pinned to SHAs with the version in a trailing comment. The cost is that security fixes to those actions now require a deliberate bump, and nothing in this repository reminds anyone. Dependabot or Renovate is the usual answer and neither is configured. Same class as the unpinned `*` dev dependencies recorded above, and probably wants solving once for both.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: `src/asgard_harness/__main__.py` remains the one file no test executes.
  evidence: Coverage is 98.89% with that module at 0%: its body only runs under `python -m asgard_harness`, which the tests reach through `cli.main` instead. It is five lines with no branch of its own, and `pixi run audit` and `selfcheck` invoke it for real in the gate, so a broken `__main__` fails CI immediately. Recorded because "98.89%" should not read as "everything ran" — which is the same conflation this story's review was about.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: A pre-commit hook was installed into .git/hooks during story 1.3 with no `.pre-commit-config.yaml` present, blocking every commit until it was uninstalled.
  evidence: Something ran `pixi run bootstrap` — the task recorded as broken in this ledger since story 1.1 — which installs the hook regardless of whether a config exists. The failure surfaced as a rejected commit that a careless read of the surrounding output would have reported as successful. Story 1.4 owns `.pre-commit-config.yaml` because the plaintext-secret rejection is its main reason to exist; when it lands, `pixi run bootstrap` becomes safe and the hook can be reinstalled deliberately. Until then `bootstrap` should not be run.
  resolution: CLOSED by story 1.4. `bootstrap` is safe to run and is verified by this story's acceptance. The failure mode itself is not forgotten: it is written up as a named failure mode on step 5 of `runbooks/l0-physical/repo-secrets.md`, with `pre-commit uninstall` as the recovery, so the next operator who hits it finds the answer in the Runbook rather than in this ledger.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-convergence-test-harness.md`
  summary: `check_layer_dependencies` is textual, so a Runbook legitimately naming a higher layer in prose fails the gate.
  evidence: It fired during story 1.3 on a sample output block inside an l0-physical Runbook that quoted push-based layer names. The document was reworded rather than the detector relaxed, which was the right call, but the detector needs a scoping rule — code and path references are the target, prose is not — rather than an exemption list that grows one entry per false positive.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-encrypted-secrets.md`
  summary: The secret scan does not read the vendored trees, and there are known plaintext credentials inside them.
  evidence: `_bmad/`, `.claude/`, `.agents/`, `.bmad-loop/`, `.pixi/`, and `.github/agents/` are excluded, mirroring the list `.yamllint` and `pyproject.toml` already exclude as "not ours to hold to our house style". The exclusion is not silent — `pixi run secrets` names it in every report — but it is real: scanning them before the exclusion produced eleven hits. Ten were the same illustrative password literal in vendored BMAD test-authoring documentation, repeated across two copies of the same skill tree; the eleventh was a Python local variable named `token`. (Neither is quoted here: this ledger is itself scanned, and a finding written out verbatim would re-create the defect it records.) All are examples rather than live credentials, and all sit in trees this story's spec marks read-only. If a real credential is ever placed in one of those trees, this check will not see it. Closing this needs either upstream to stop shipping credential-shaped examples or a per-tree allowlist, which is the shape that grows one entry per false positive and was deliberately not started.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-encrypted-secrets.md`
  summary: The escrow record names three holders and no key. Two of its three dates are blank because nothing has been escrowed.
  evidence: `runbooks/l0-physical/repo-secrets.md` § Escrow enumerates where the age identity is held and how it rotates, satisfying AD-24's "outside any single un-escrowed machine" by naming two holders that survive the loss of the control node. But no identity exists: generating one at authoring time would have put the only copy of its private half on one machine, which is the exact failure the rule names, so `.sops.yaml` ships the `AGE-RECIPIENT-UNSET` sentinel instead. The record therefore states its own current state explicitly rather than leaving the blank dates to be read as an oversight. The holders themselves — a password manager entry and an off-site offline copy — are a specification the operator has not yet confirmed they can honour; the first execution of step 2 either confirms them or changes them, and that is a human decision, not a defect the harness can find.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-encrypted-secrets.md`
  summary: The plaintext scan is pattern-based, so a credential with no recognisable shape passes it.
  evidence: Eight patterns match credential *formats* — PEM and OpenSSH private-key armour, age identities, AWS key identifiers, GitHub and Slack tokens, JWTs, `sk-` provider keys — plus one heuristic for a credential-shaped value assigned to a credential-shaped field. An entropy scanner was rejected on purpose: it fires on lock files, checksums, and base64 test data, and a check that cries wolf is a check people learn to skip. The residual gap is real and is the reason encryption is *also* declarative: a file the `.sops.yaml` policy covers must be encrypted regardless of what is in it, so the two rules cover different halves. What neither covers is a weak, shapeless password in a file the policy does not name.
  pattern: The keyword boundary was got wrong twice, and the value predicate once, each time passing a fixture written to the same assumption as the code. (1) `\b` treats `_` as a word character, so `directory_bind_password` never matched. (2) The `(?<![A-Za-z0-9])` that fixed it broke `bindPassword`, because camelCase puts a letter immediately before the keyword. (3) `_PLACEHOLDER_MARKERS` contained credential *field names* — `token`, `secret`, `password` — and the membership test is `in`, so every real credential containing one of those words was silently exempted; `s3cr3t-not-a-real-token-abcdef123456` was skipped because it contains `token`. Review caught (2) and (3) against the live repository after this story reported them fixed. The standing fix is structural: there is now one fixture per naming *shape* (`UPPER_SNAKE=`, `lower_snake:`, `camelCase =`, quoted, unquoted, `export`, prefixed) rather than one standing in for the class, plus a test asserting no value is ever exempted for containing a credential word. Same class as this project's recurring defect, with a sharper edge: a fixture written by the author of the bug tests the bug's assumption, not the requirement.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-encrypted-secrets.md`
  summary: `go-sops` and `age` are new dev dependencies, pinned to ranges rather than exactly, and nothing in this Repository is yet encrypted by them.
  evidence: `docs/OWNERSHIP.md` names `SOPS + age` as the owner of Repository-stored secret material, so a Runbook whose commands could not be run would have been the alternative. conda-forge packages SOPS as `go-sops`; the executable is `sops`. They are pinned `>=3.13.3,<4` and `>=1.3.1,<2` — the same range style the existing runtime pins use, and not `latest`, but AD-20 says pins are exact and these are not. Same class as the `*`-pinned dev tools already recorded above, and it wants solving once for all of them rather than one dependency at a time.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-encrypted-secrets.md`
  summary: Every harness run now prints a `RuntimeWarning` about the GIL to stderr, because `ruamel.yaml`'s C extension does not declare free-threading support.
  evidence: The default environment is a Python 3.14 free-threading build, and this story is the first code to import `ruamel.yaml` — a dependency declared since the workspace commit and until now unused. CPython re-enables the GIL to load `_ruamel_yaml` and says so on stderr. Functionally harmless: the interpreter is correct, only slower, and the report itself is unaffected. It is noise in the output of a gate whose whole discipline is that its output means something. Options are an upstream fix, `PYTHON_GIL=0` (which is the "at your own risk" branch and was not taken), or dropping to a pure-Python YAML load.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-encrypted-secrets.md`
  summary: The self-check's fixture subject has to be re-pointed by hand every time its owning story lands, and this is the second time.
  evidence: `selfcheck.SUBJECT_KEY` names one `planned` Index entry with neither half on disk; twelve fixtures mutate it. Story 1.3 pointed it at `PROC-REPO-SECRETS` because story 1.4 was unstarted; story 1.4 building it made all twelve inert, so it now points at `PROC-REBUILD-DRILL` (story 18.2, the last story of the last epic). Mitigated rather than closed: the key and its Runbook path are two constants that everything else derives from, and `test_the_selfcheck_subject_entry_is_still_inert` fails with the reason rather than leaving the next author to infer it from twelve red fixtures. A structural fix would select the subject at run time from the Index — the first `planned` entry with neither half present — which removes the trap entirely but makes each fixture's target something a reader has to compute rather than read.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-encrypted-secrets.md`
  summary: A pre-commit hook cannot invoke anything that is not already committed, and `pixi run` turns that into a silent block on every commit.
  evidence: pre-commit reverts unstaged changes before running hooks, so a hook sees the staged `pixi.toml`, not the working tree. `pixi run <name>` runs the task called <name> if the manifest declares one and OTHERWISE RUNS <name> AS A SHELL COMMAND — so an unresolvable task exits 127 with `<name>: command not found`, and pre-commit renders that identically to the scan finding a credential: `Failed`, exit 1, commit rejected. Every commit blocked, nothing checked, and the rejection looks exactly like the check working. It also has a second-order effect: the hook's `pixi run` re-solves the environment from the reverted manifest and deletes dependencies the working tree had added, which is how `sops` and `age` vanished mid-test-run. Closed for the steady state by `test_every_hook_entry_names_a_task_the_COMMITTED_manifest_declares`, which fails naming the task and the reason. NOT closed in general: no hook can be robust against its own implementation being uncommitted, so `pixi.toml`, `.pre-commit-config.yaml`, and the harness code that hooks invoke must always land in one commit. A structural fix would need pixi to offer task-only resolution, so that a missing task is an error rather than a shell fallback.
  pattern: The direction that was never tested. This story verified that a commit carrying a credential is rejected and concluded detection worked — but a broken hook rejects a clean commit identically, so the rejection carried no information. Worse, the verification staged everything first, which left pre-commit nothing to revert and removed the only condition under which the bug appears. Every check needs both directions: that it fires on bad input, and that it stays quiet on good input. A check tested only on bad input is satisfied by a check that fires on everything.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-encrypted-secrets.md`
  summary: The secret scan now covers untracked files, which closes a hole but can block a commit over a scratch file the developer never intended to commit.
  evidence: The scan originally enumerated `git ls-files` — tracked paths only — so a developer could write a file holding a credential, run `pixi run secrets`, and be told the Repository was clean because the file was not tracked *yet*. It now enumerates `--cached --others --exclude-standard`: everything git tracks plus everything it would track, with gitignored paths excluded. The scope is stated verbatim in every report. The cost is that an untracked scratch file carrying a credential now fails the gate and blocks commits until it is removed or gitignored — which is the intended answer, but it is a behaviour change a developer meets without warning. `.gitignore` is the declared way out, which keeps the exclusion a committed decision rather than an accident of not having run `git add`.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-address-plan.md`
  summary: The address plan's declared verification has two halves and only one is built. Reconciliation against DNS and against what hosts answer is owed by stories 4.3 and 2.3.
  evidence: `docs/OWNERSHIP.md` names the class's verification as "declared addresses reconciled against Directory DNS and against what hosts actually answer". Neither is available: the zone does not exist until story 4.3 and no host answers until story 2.3. What story 2.1 built is the internal-consistency check — collisions, statics inside the DHCP pool, a Node on one segment only, a consumed reservation, a route on the isolated segment, an illegal kind, ranges that do not tile their segment, an address in no declared range. That is a real claim and a narrow one: the plan agrees with itself, not with the network. Both documents now state the split explicitly rather than carrying one sentence that would read as complete the moment the first half landed. Recorded here rather than dropped, per the spec's own Design Note. The natural home is story 2.3 for the host half and 4.3 for the DNS half; each should add its half of the check in the change that first makes it possible.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-address-plan.md`
  summary: The household router is in a closet and is not on the UPS, which contradicts the power design's outlet allocation.
  evidence: The spec's Confirmed facts state it as measured. The addendum's outlet budget assigns UPS outlet 8 to the household router precisely so that FR-59's power alert can leave the premises during the event that triggers it, and it already notes the ISP equipment may be a separate modem and router — a ninth device against eight outlets. Being in a closet makes that allocation a plan rather than a fact, and an outbound alert that cannot leave is the failure mode the outlet was bought for. Not this story's to resolve: story 15.1 owns the power inventory and the outlet allocation. Recorded in `docs/ADDRESS-PLAN.md` § Confirmed facts as well, because the address plan is where the router's existence is declared and a reader arriving from an address should meet the caveat there.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-address-plan.md`
  summary: `docs/OWNERSHIP.md` carries a resource-class row for the TL-WA3001 wireless bridge, which the addendum records as not deployed and removed from the topology.
  evidence: The L0 table's "Wireless bridge (TL-WA3001, client mode)" row names `PROC-ADDRESS-PLAN` as its covering Procedure, so this story is the first that had to decide what address it holds. The answer is none: addendum section 1 marks the unit **Not deployed** — it served household wireless coverage, never Asgard, and was removed to free a switch port. The address plan therefore declares no allocation for it, which leaves an ownership row for a device that is not in the platform. Removing an ownership row is a human decision about the ownership table, not a change this story's spec authorises, so it is surfaced rather than made. Two clean resolutions: delete the row, or keep it and record the device as deliberately unaddressed. Either way the row and the plan should stop disagreeing.
  resolution: CLOSED by story 2.1, and the finding resolved toward the STALE document. The decision had been reversed — the TL-WA3001 is deployed, runs in client-bridge mode, and is the platform's **only** uplink: the rack is on the opposite side of the house from the household router, with no spare wired path and no coaxial for MoCA. That reversal had been propagated to the PRD and `brief.md` and missed in the addendum, which is the copy this story read. The human corrected the addendum in four places (inventory row, topology diagram, two-switch rationale, power load model) and the plan now declares the bridge at `192.168.86.2`, `Kind: network device`, in a new `data-infrastructure` allocatable range split out of what had been a pure growth reservation — the narrow-the-reservation-in-the-same-change operation the plan documents. The ownership row stays and is reconciled: it is no longer a device the plan declines to address. What remains true and narrower is already recorded in the first entry of this story above: the plan cannot yet verify that the bridge answers on the address it declares, because that is the reconciliation half owed by stories 2.3 and 4.3. No new entry is added for it, because a second row for one gap is how a ledger stops being readable as a work list.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-address-plan.md`
  summary: The plan declares each address's physical interface, but nothing yet pins the OS-level interface name the USB 2.5 GbE adapters will present.
  evidence: The addendum flags it directly — "Verify deterministic interface naming on arrival: USB NIC naming is less stable than onboard and the address plan depends on it." The plan names the interface physically (`USB 2.5 GbE adapter`, `Onboard 1 GbE (RJ45)`), which is what makes an address traceable to a port and is deliberately as far as a declaration can go before the hardware exists. The binding from that physical interface to a name a configuration file can use belongs to story 2.3, and if the adapters renumber across reboots the fix is a naming rule (a systemd link file or a MAC-keyed match), not an edit to this plan. Flagged so 2.3 meets it as a stated requirement rather than as a surprise.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-address-plan.md`
  summary: The shared-last-octet convention is asserted by a test, not by a detector, and that asymmetry is deliberate but worth stating.
  evidence: The spec's Design Note asks that a Node's last octet be identical on both segments. It is a readability convention rather than a correctness rule — a future host could legitimately need different octets — so making it a defect class would have turned a helpful habit into a rule someone has to argue their way out of. It is pinned instead by `test_every_node_carries_one_address_on_each_segment_with_a_matching_last_octet`, which fails naming the host. If a Node ever legitimately needs mismatched octets, the test is the thing to change, and changing it is visible in review.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-address-plan.md`
  summary: Nothing checks a planning artifact against another planning artifact, which is how a decision reversed in three documents stayed wrong in a fourth.
  evidence: The harness's cross-document rules all run downhill from the story list: `PROCEDURE-INDEX.md` is checked against `epics.md`, `docs/OWNERSHIP.md` against the Index, Runbooks against both. The brief's addendum is checked against nothing, so when the wireless bridge's deployment decision was reversed and propagated to the PRD and `brief.md`, the addendum kept the old claim and this story read it and declared the device unaddressed. The harness structurally could not catch it. Scope the fix honestly: NOT "check every document against every other", which is unbounded and would produce a checker that is mostly false positives. The finite, mechanical version is that the addendum's **inventory and topology claims** should agree with `docs/ADDRESS-PLAN.md` and `docs/OWNERSHIP.md` — every device the inventory marks deployed has an ownership row, and every device with a management address in the plan is one the inventory marks deployed. Both sides are tables; the join key is the device. Left open rather than started here: it needs the addendum's tables to carry a stable device identifier, which is a change to a planning artifact this story's spec marks read-only.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-address-plan.md`
  summary: The addendum's power section is now half-corrected for the wireless bridge: the load model names it but does not count it, and the outlet budget still reasons from its removal.
  evidence: Surfaced while reconciling the bridge into the address plan. Section 9's load-model note reads "The access point is powered as the uplink bridge and is included above", but the table above it has no row for the bridge and still totals ~320 W typical / ~430 W peak — the figures from when it was not deployed. The outlet budget then says "The access point's removal and the membership switch's addition cancel out", which is the reversed decision, and its eight-outlet table has no entry for the bridge. This matters beyond arithmetic: the bridge is now the only outbound path, so FR-59's power alert cannot leave the premises without it — exactly the argument that put the household router on outlet 8. Against eight outlets already fully allocated, the bridge is a ninth device, joining the separate-modem case the section already flags. Not this story's to fix and not this story's document: story 15.1 owns the power inventory and the outlet allocation, and both figures have to move together. Recorded rather than edited, because the addendum is a planning artifact this story's spec marks read-only.
