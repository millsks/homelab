---
topic: 4-node Norse-named homelab platform: k8s + stateful services + centralized identity/SSO + shared storage, documented for manual and automated build/rebuild
updated: 2026-08-28T19:15
---

- (event) Create intent confirmed; workspace seeded. Source input: infrastructure_inventory.txt + user goal dump.
- (decision) Naming convention is Norse mythology for all nodes/hosts — user requirement, non-negotiable.
- (assumption) Inventory gaps observed: no dedicated router/firewall (only GL.iNet travel router + TP-Link AP), no UPS, nodes have 1GbE onboard so the switch's 8x2.5G + 2x10G ports are largely unusable without added NICs. Needs user confirmation.
- (override) CLAUDE.md mandates pixi as sole Python runner; repo has no pixi manifest and BMAD scripts are stdlib-only PEP723 helpers. Using python3 directly instead of uv run — avoids the competing-.venv failure mode the rule targets.
- (decision) Working mode: Coaching path.
- (decision) Edge: existing ISP router stays; lab hangs off flat LAN. No VLAN segmentation in scope for v1. No new firewall purchase.
- (decision) Users/exposure: single admin user (Kevin). Nothing internet-facing; remote access via VPN. Internal CA acceptable; no ACME/public DNS in v1.
- (decision) Node foundation: Proxmox VE on all 4 EliteDesks. K8s nodes, service VMs and identity run as VMs/LXC. Chosen for snapshot + fast-rebuild alignment.
- (decision) Purpose ranking: mix of A (enterprise simulator) + B (skills investment) + D (platform for user's own apps). Keycloak justified primarily by practicing OIDC/PingFed-style authN/authZ in apps the user builds.
- (decision) Keycloak scope expanded beyond app-auth: also fronts k8s API (OIDC), Proxmox, Grafana, Argo CD, Synology DSM, and dumb apps via oauth2-proxy forward-auth. Federates to LDAP so network logins + SSO are ONE identity system.
- (decision) VPN stays a separate plane (Keycloak cannot replace it). Headscale+OIDC noted as the stretch option; plain WireGuard/Tailscale acceptable for v1.
- (decision) Bootstrap paradox solution: local emergency admin on every node + SSSD cache_credentials + Proxmox root@pam + physical console as last resort. Second directory replica = should-have, not must-have.
- (decision) K8s persistent volumes come from NFS on the Synology (nidavellir). User accepts NAS reboot impact.
- (decision) CARVE-OUT: PostgreSQL does NOT run on NFS. Runs as a VM on local NVMe, or on an iSCSI LUN via synology-csi. Postgres fsync/locking semantics vs NFS is the reason.
- (decision) Norse naming scheme locked: realms=tiers, beings=instances. Nodes odin/thor/heimdall/tyr; cluster yggdrasil; control plane urd/verdandi/skuld; workers brynhildr/sigrun/gondul; storage nidavellir; LDAP mimir; Keycloak forseti; Postgres fafnir; Redis ratatoskr; observability huginn/muninn; CA draupnir; Vault andvari; ingress bifrost; CI brokkr/sindri; backup hel.
- (assumption) Nodes are 1GbE onboard, so all NFS/storage traffic caps ~110MB/s. Accepted as the first ceiling this build will hit.
- (assumption) EliteDesk 705 G5 Mini has no IPMI/iLO - no out-of-band management. Physical console is the true last-resort recovery path.
- (event) Finalize audit: all 17 memlog entries accounted for. Decisions -> brief; supporting depth -> addendum (inventory assessment, capacity model, naming registry, identity design, storage notes, doc model, candidate epics, deferred/rejected table). Process noise (create intent, coaching-path choice, pixi/uv override) intentionally not carried into either doc.
- (override) Brief runs ~1700 words vs the skill's 1-2 page guidance. Kept deliberately: every section is load-bearing for downstream PRD/architecture and the tables read fast. Detail that could be cut was already pushed to addendum.md.
- (decision) Candidate epic decomposition drafted (15 epics, 0-14) in addendum sec.7 for the PRD to confirm or restructure. Epic 14 (reference OIDC app) validates the lab's purpose; Epic 13 (destructive rebuild test) validates the documentation.
- (decision) DNS domain: asgard.home.arpa (RFC 8375 reserved). Internal CA draupnir issues all TLS; no public DNS.
- (decision) Kubernetes distribution: k3s. HA via 3 embedded-etcd servers (urd/verdandi/skuld). Bundled Traefik/ServiceLB keep-or-replace deferred to architecture.
- (decision) Automation: Ansible for configuration management, OpenTofu for Proxmox provisioning.
- (decision) UPS accepted into scope: APC 1500VA/900W. User proposed BX1500M; analyst recommends BR1500MS (pure sine wave) since all gear is external-brick powered but the NAS holds all persistent state. Model choice still pending user.
- (decision) Power design constraint discovered: NAS must shut down LAST (VM storage depends on it) yet is the natural NUT server. Switch must also be on the UPS or clients lose network before the shutdown signal. New epic 12b; verified by live pull-the-plug drill.
- (change) Brief updated: Open Questions section replaced with Resolved Foundation Decisions + 4 items deferred to architecture (directory product, Postgres placement, RAID level, k3s ingress). UPS added to scope; constraints row rewritten from 'No UPS' to 'Power runtime'.
- (event) Brief + addendum marked approved; all discovery open questions resolved. Handing off to bmad-prd.
- (decision) UPS SETTLED: APC Back-UPS Pro BR1500MS2 (pure sine wave), upgraded from the BX1500M. Removes transfer-time risk class under the NAS.
- (assumption) Outlet constraint found: BR1500MS2 has 10 outlets but only 5 battery-backed, vs 6-7 devices needing backup. Resolution: non-surge power-distribution strip on one battery outlet feeding the 4 mini-PC bricks (~200W). Cascading surge suppressors is an anti-pattern. Switch MUST be battery-backed or NUT clients lose network before the shutdown signal.
- (decision) UPS FINAL: APC Smart-UPS SMT1500C (1500VA/1000W, pure sine, 8 outlets ALL battery-backed, hot-swap battery). Third and final selection - BX1500M rejected on waveform, BR1500MS2 rejected on outlet topology (5 battery of 10 vs 6 required). Smart-UPS resolves outlet count structurally; distribution strip no longer needed.
- (decision) Token lifetime: 15 minutes. Access-token validity window for OIDC Relying Parties; bounds how long a disabled Account retains access (UJ-5). Becomes a numbered FR in PRD sec 4.7.
- (assumption) OPEN GATE: SMT1500C must be verified against Synology DSM's supported UPS list before purchase - DSM is the NUT server, so incompatibility forces a topology change, not a workaround.
- (change) Brief + addendum synced with the remote-access cut and the wired-only constraint. Addendum gains section 10 Network Topology.
- (decision) Hardware: 4x UGREEN USB-A 2.5GbE adapters purchased. Traffic split CORRECTED from initial advice - bulk (NFS/pods/backup) goes on the USB 2.5GbE adapters, cluster membership stays on the ONBOARD 1GbE. Rationale: USB Ethernet is prone to resets/renumbering; acceptable for traffic that retries, not for the signalling that decides whether a node is alive.
- (decision) TP-Link AP NOT DEPLOYED - served household wireless coverage only, never Asgard. Frees a switch port. Reintroduce only on household coverage grounds.
- (decision) Second switch added: ~USD15 5-port GbE dedicated to cluster membership, ISOLATED with NO uplink, own subnet, no gateway. Driver is port headroom (adapters took node demand from 1 to 2 ports = 11 needed vs 10 available), not fabric contention. Data switch drops to 6/10 with 4 spare.
- (assumption) OPEN: is the ISP equipment a combined modem-router or separate modem + router? If separate, both need battery power for FR-59 alerts to leave the premises - that is 9 devices against 8 outlets. Resolution: plain non-surge distribution strip on outlet 8 for the two small network devices.
- (decision) Membership switch SETTLED: TP-Link Omada ES205G (5-port gigabit easy-managed, fanless, metal, ~2-3W). Easy-managed chosen over unmanaged for port error counters and cable diagnostics on the segment whose failures are hardest to diagnose. Accepted cost: it becomes a configuration item under FR-4.
- (decision) HARD CONSTRAINT: ES205G runs STANDALONE, never adopted into an Omada controller. Adoption would make the cluster-membership network's config dependent on controller software - circular dependency if that controller ever runs as a workload on the cluster it serves. Same failure class as the VPN/IdP circularity cut earlier.
- (decision) Membership switch settings: static mgmt IP on membership subnet with no gateway; IGMP snooping OFF (corosync 3/knet is unicast; snooping without a querier can silently break multicast); VLANs/QoS/LAG unused. EEE/802.3az disabled if the UI exposes it, else monitor corosync latency after cutover.
